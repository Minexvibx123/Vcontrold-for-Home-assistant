name: Validate Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Validate manifest.json
        run: |
          python3 -m json.tool vcontrold/manifest.json > /dev/null
          echo "✅ manifest.json is valid"
